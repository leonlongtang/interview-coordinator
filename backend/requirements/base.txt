Django>=5.0,<6.0
djangorestframework>=3.15,<4.0

# PostgreSQL
psycopg[binary]>=3.1,<4.0

# Environment variables
python-dotenv>=1.0,<2.0

# CORS
django-cors-headers>=4.3,<5.0

# Security
django-filter>=23.5,<24.0

# Utilities
pytz

# Authentication - JWT tokens for stateless auth
djangorestframework-simplejwt>=5.3,<6.0
# Authentication - provides login/logout/register endpoints out of the box
dj-rest-auth>=6.0,<7.0
# Authentication - handles user registration, email verification, social auth
django-allauth>=64.0,<65.0
# HTTP library - required by django-allauth for OAuth/social auth
requests>=2.31,<3.0

# Task queue - Celery for background jobs (email reminders, etc.)
celery>=5.3,<6.0
# Redis - message broker for Celery
redis>=5.0,<6.0
# Celery Beat - scheduled task support (periodic reminders)
django-celery-beat>=2.6,<3.0